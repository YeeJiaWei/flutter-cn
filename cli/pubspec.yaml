name: fcn
description: CLI to add flutter-snippets components into a project, shadcn-style.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

executables:
  fcn: fcn
